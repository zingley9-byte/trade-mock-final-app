/**
 * AdBanner — Web stub
 *
 * Metro uses this file for web builds instead of AdBanner.tsx.
 * react-native-google-mobile-ads is a native-only module and must never
 * be bundled for web — this stub ensures it isn't even imported.
 */
import React from "react";

export default function AdBanner() {
  return null;
}
