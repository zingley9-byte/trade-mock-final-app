/**
 * InterstitialAdManager — Web stub
 *
 * Metro uses this file for web builds instead of InterstitialAdManager.ts.
 * react-native-google-mobile-ads is a native-only module and must never
 * be bundled for web — this stub exports a no-op singleton with the same
 * interface so all call sites compile and run without changes.
 */

const interstitialAd = {
  preload() {},
  tryShow() {},
};

export default interstitialAd;
